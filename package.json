{
  "name": "agentic-ai",
  "version": "1.0.0",
  "description": "Playwright URL validation test application",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "docker:test": "docker build -t playwright-url-validator . && docker run --rm playwright-url-validator"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.1",
    "playwright": "^1.60.0",
    "typescript": "^6.0.3"
  }
}
